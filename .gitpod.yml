checkoutLocation: src/github.com/hpssjellis/gitpod-hello-ui-demo
workspaceLocation: /
image:
  file: Dockerfile
ports:
  - port: 6080
    protocol: "http"
tasks:
  - command: "go get -v ./... && go run main.go"
  
  
  
  
  
